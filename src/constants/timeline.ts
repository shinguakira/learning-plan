/** Width of the sticky task-name column, in pixels. */
export const LABEL_W = 248

/** Width of one day column, in pixels. */
export const DAY_W = 34

/** Height of one task row, in pixels. */
export const ROW_H = 46

/** Days of padding added on each side of the task date range. */
export const RANGE_PADDING_DAYS = 2

/** A shorter range than this looks empty, so the axis is stretched to it. */
export const MIN_RANGE_DAYS = 14

/** Below this bar width the task title is dropped rather than truncated. */
export const MIN_BAR_LABEL_W = 64
